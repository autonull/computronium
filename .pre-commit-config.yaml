repos:
  - repo: local
    hooks:
      - id: ruff-format
        name: ruff format
        entry: uv run ruff format
        language: system
        types: [python]
      - id: ruff-check
        name: ruff check
        entry: uv run ruff check --fix
        language: system
        types: [python]
      - id: pyright
        name: pyright (ontology strict gate)
        entry: uv run pyright computronium/ontology
        language: system
        types: [python]
        pass_filenames: false
        always_run: true
      - id: identity-cards
        name: identity cards (C.1: every concrete primitive carded)
        entry: uv run python scripts/generate_identity_cards.py --strict
        language: system
        pass_filenames: false
        always_run: true
      - id: pytest-fast
        name: pytest (property tests)
        entry: uv run pytest tests/property/ -q
        language: system
        pass_filenames: false
        always_run: true